Step 1: Download the folder containing all the files


Step 2: Go into the correct directory containing all the files:


cd hdb_price_predictor


Step 3: Run code


python -m streamlit run app.py (To see both prediction and evaluation models)
python -m streamlit run app_prediction.py (To see prediction)
python -m streamlit run app_evaluation.py (To see evaluation models)